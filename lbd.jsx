import React from 'react';
import ReactDOM from 'react-dom';

//let score=JSON.parse(JSON.fetch("scores.json"))

const a=document.getElementById('leaderboard_container')
 
const leaderboard=(	
	<div>
		<span>1</span>
		<span>1</span>
		<span>3</span>
		<span>4</span>
		<span>5</span>

	</div>
);

ReactDOM.render(leaderBoard,a)
